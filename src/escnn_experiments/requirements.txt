# Dependencies for the escnn-based equivariance-metric experiments.
#
# Kept separate from the repo-root requirements.txt because escnn needs
# lie_learn, and lie_learn pins numpy<2 -- which conflicts with the
# numpy==2.2.6 the main environment uses. See README.md.

torch==2.10.0
torchvision==0.25.0

# escnn was already listed at the repo root, but is unusable there without
# lie_learn: `import escnn` fails with ModuleNotFoundError, because
# escnn.group imports its SO(3) machinery unconditionally.
escnn==1.0.11
lie_learn==0.0.2
py3nj==0.2.1

# lie_learn requires numpy<2. This is the whole reason for the split.
numpy==1.26.4

# scipy: spearmanr, for the RSA metric in equivariance_metrics_comparison.py
scipy==1.15.3
